<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.feevale.peneirao">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastrarUsuario" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".PosicaoActivity"
            android:label="@string/title_activity_posicao" />
        <activity
            android:name=".CadastrarPosicao"
            android:label="@string/title_activity_cadastrar_posicao" />
        <activity
            android:name=".ClubeActivity"
            android:label="@string/title_activity_clube" />
        <activity
            android:name=".CadastrarClube"
            android:label="@string/title_activity_cadastrar_clube"></activity>
        <activity
            android:name=".AvaliacaoActivity"
            android:label="Avaliação"></activity>
        <activity
            android:name=".CadastrarAvaliacao"
            android:label="Cadastrar Avaliação"></activity>
        <activity
            android:name=".AtletaActivity"
            android:label="Atletas"></activity>
        <activity
            android:name=".CadastrarAtleta"
            android:label="Cadastrar Atleta" />
    </application>

</manifest>